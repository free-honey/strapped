{
  "$schema": "https://schema.railpack.com",
  "caches": {
    "npm-cache": {
      "directory": "~/.npm",
      "type": "locked"
    }
  },
  "steps": {
    "build": {
      "caches": [
        "npm-cache"
      ],
      "commands": [
        "npm run build"
      ]
    }
  },
  "deploy": {
    "inputs": [
      {
        "step": "build",
        "include": [
          "dist"
        ]
      }
    ],
    "startCommand": "npm run preview -- --host 0.0.0.0 --port ${PORT}"
  }
}
