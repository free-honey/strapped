import { Transaction, VersionedTransaction } from '@solana/web3.js';
import type { ConfirmOptions, Signer } from '@solana/web3.js';
import type { Adapter } from '@solana/wallet-adapter-base';
export declare function signAndSendTransaction(adapter: Adapter): (transactionParam: Transaction | VersionedTransaction, signers: Signer[], confirmOptions?: ConfirmOptions) => Promise<string>;
