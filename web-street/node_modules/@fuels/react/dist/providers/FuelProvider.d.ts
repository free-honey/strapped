import type { FuelConfig } from 'fuels';
import type { NetworkConfig, UIConfig } from '../types';
import { type FuelUIProviderProps } from './FuelUIProvider';
export { useFuel } from './FuelHooksProvider';
export { useConnectUI } from './FuelUIProvider';
type FuelProviderProps = {
    ui?: boolean;
    uiConfig?: UIConfig;
    fuelConfig: FuelConfig;
    networks?: Array<NetworkConfig>;
} & Pick<FuelUIProviderProps, 'theme' | 'children'>;
export declare function FuelProvider({ theme: _theme, children, fuelConfig, uiConfig: _uiConfig, ui, networks: _networks, }: FuelProviderProps): import("react/jsx-runtime").JSX.Element;
