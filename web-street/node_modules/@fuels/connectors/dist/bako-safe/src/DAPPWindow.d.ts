type PopupConfig = {
    appUrl: string;
    height: number;
    width: number;
    sessionId: string;
    request_id: string;
};
export declare class DAppWindow {
    private config;
    isMobile: boolean;
    isSafariBrowser: boolean;
    request_id: string;
    isOpen: boolean;
    opned: Window | null;
    constructor(config: PopupConfig);
    private get popupConfig();
    open(method: string, reject: (e: Error) => void): void;
    close(): void;
    makeLink(method: string): void;
    makeFrame(method: string, isSafari?: boolean): void;
    makePopup(method: string): any;
    private get queryString();
    private get small();
}
export {};
