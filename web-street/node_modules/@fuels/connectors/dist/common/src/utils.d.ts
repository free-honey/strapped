import type { Hex } from 'viem';
import type { Maybe, PredicateConfig } from './types';
export declare const getOrThrow: <T>(value: Maybe<T>, message: string) => T;
export declare const getFuelPredicateAddresses: ({ signerAddress, predicate: { abi, bin }, }: {
    signerAddress: string;
    predicate: PredicateConfig;
}) => Hex;
