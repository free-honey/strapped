import { type AbiMap, type Asset, type ConnectorMetadata, FuelConnector, FuelConnectorEventTypes, type HashableMessage, type JsonAbi, type Network, type SelectNetworkArguments, type TransactionRequestLike, type TransactionResponse, type Version } from 'fuels';
import type { BurnerWalletConfig } from './types';
export declare class BurnerWalletConnector extends FuelConnector {
    name: string;
    connected: boolean;
    installed: boolean;
    external: boolean;
    events: typeof FuelConnectorEventTypes;
    metadata: ConnectorMetadata;
    private burnerWallet;
    private fuelProvider;
    private storage;
    private config;
    constructor(config?: BurnerWalletConfig);
    private configProvider;
    private getProvider;
    private generatePrivateKey;
    private getPrivateKey;
    private getStatus;
    private setupBurnerWallet;
    private getStorage;
    /**
     * ============================================================
     * Connector methods
     * ============================================================
     */
    ping(): Promise<boolean>;
    version(): Promise<Version>;
    isConnected(): Promise<boolean>;
    connect(): Promise<boolean>;
    accounts(): Promise<string[]>;
    disconnect(): Promise<boolean>;
    signMessage(address: string, message: HashableMessage): Promise<string>;
    sendTransaction(_address: string, transaction: TransactionRequestLike): Promise<string | TransactionResponse>;
    currentAccount(): Promise<string | null>;
    addAssets(_assets: Asset[]): Promise<boolean>;
    addAsset(_asset: Asset): Promise<boolean>;
    assets(): Promise<Array<Asset>>;
    addNetwork(_networkUrl: string): Promise<boolean>;
    selectNetwork(_network: SelectNetworkArguments): Promise<boolean>;
    networks(): Promise<Network[]>;
    currentNetwork(): Promise<Network>;
    addAbi(_abiMap: AbiMap): Promise<boolean>;
    getAbi(_contractId: string): Promise<JsonAbi>;
    hasAbi(_contractId: string): Promise<boolean>;
}
