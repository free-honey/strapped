import { type JsonAbi } from '@fuel-ts/abi-coder';
import { type BN } from '@fuel-ts/math';
import type { ReceiptCall } from '@fuel-ts/transactions';
type GetFunctionCallProps = {
    abi: JsonAbi;
    receipt: ReceiptCall;
    offset: number;
    scriptData: Uint8Array;
};
export interface FunctionCall {
    amount?: BN | undefined;
    assetId?: string | undefined;
    functionSignature: string;
    functionName: string;
    argumentsProvided: Record<string, unknown> | undefined;
}
/**
 * Builds a FunctionCall object from a call receipt.
 */
export declare const getFunctionCall: ({ abi, receipt, offset, scriptData, }: GetFunctionCallProps) => FunctionCall;
export {};
//# sourceMappingURL=call.d.ts.map